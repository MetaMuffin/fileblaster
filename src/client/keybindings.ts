import { EventEmitter } from "events"



export class Keybindings {
    static keymap: any
    static keyEmitter: EventEmitter = new EventEmitter()

    static init() {
    }

    static bindElementClick(element: HTMLElement) {
        
    }
}
